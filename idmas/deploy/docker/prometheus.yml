# Prometheus 抓取配置（IDMAS）
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: idmas-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8080"]

  - job_name: idmas-worker
    metrics_path: /metrics
    static_configs:
      - targets: ["worker:9100"]
